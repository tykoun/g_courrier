{% extends "base.html.twig" %}
{% block title %} Ajout des Service {% endblock %}

{% block body %}
    <div class="container-fluid">

        <div class="row">
            <div class="col-sm-6 offset-3 mt-2">
                <h3>Ajout de Service</h3>

                {{ form_start(form) }}
                {{ form_widget(form) }}
                <button class="btn btn-primary">Enregistrer</button>
                {{ form_end(form) }}
            </div>
        </div>


    </div>
{% endblock %}
{% extends "base.html.twig" %}
{% block title %} Ajout des dossier{% endblock %}

{% block body %}
    <div class="container-fluid">
        <div class="row">
            <div class="col-sm-6 mt-2 offset-3">
                <h3>Ajout de dossier</h3>

                {{ form_start(form) }}
                {{ form_widget(form) }}

                <button class="btn btn-primary">Enregistrer</button>
                {{ form_end(form) }}
            </div>
        </div>

    </div>
{% endblock %}